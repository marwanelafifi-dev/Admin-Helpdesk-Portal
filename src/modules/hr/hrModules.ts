/**
 * Every module id that feeds into the HR Team's unified "All Requests" view
 * (src/app/departments/hr/all-requests/page.tsx). Add a new HR module's id
 * here once it's built — no other wiring needed for it to show up there.
 */
export const HR_MODULE_IDS = ["hr", "hr_general"]
